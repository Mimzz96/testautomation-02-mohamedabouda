{
  "name": "frontend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run --reporter mochawesome",
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:vrt": "percy exec cypress run --spec cypress/integration/TestSuite.spec.js"

  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@percy/cli": "^1.0.0-beta.48",
    "@percy/cypress": "^3.1.0",
    "cypress": "^7.2.0"
  },
  "dependencies": {
    "mocha": "^5.2.0",
    "mochawesome": "^6.2.2",
    "mochawesome-merge": "^4.2.0"
  }
}
